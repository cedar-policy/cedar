name: Cargo Build & Test

on:
  pull_request:

env:
  CARGO_TERM_COLOR: always
jobs:
  build_and_test:
    name: Rust project - latest
    runs-on: ubuntu-latest
    strategy:
      matrix:
        toolchain:
          - stable
    steps:
      - uses: actions/checkout@v3
      - run: rustup update ${{ matrix.toolchain }} && rustup default ${{ matrix.toolchain }}
      - run: cargo fmt --all --check
      - run: RUSTFLAGS="-D warnings -F unsafe-code" cargo build --verbose
      - run: cargo test --verbose
      - run: cargo test --verbose -- --ignored
      - run: cargo bench --no-run

  cargo_audit:
    name: Cargo Audit
    runs-on: ubuntu-latest
    strategy:
      matrix:
        toolchain:
          - stable
    steps:
      - uses: actions/checkout@v3
      - run: rustup update ${{ matrix.toolchain }} && rustup default ${{ matrix.toolchain }}
      - run: cargo install cargo-audit
      - run: cargo audit --deny warnings --ignore RUSTSEC-2021-0145

  cargo_semver_checks:
    name: Cargo SemVer Checks
    runs-on: ubuntu-latest
    continue-on-error: true
    strategy:
      matrix:
        toolchain:
          - stable
    steps:
      - uses: actions/checkout@v3
        with:
          path: head
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.base_ref }}
          path: base
      # `cargo semver-checks` doesn't understand `rlib` crates.
      - run: sed -i 's/^crate_type = \["rlib"\]$/crate_type = ["lib"]/' {head,base}/cedar-policy/Cargo.toml
      - run: rustup update ${{ matrix.toolchain }} && rustup default ${{ matrix.toolchain }}
      - run: cargo install cargo-semver-checks
      - run: cargo semver-checks check-release --baseline-root ../base --package cedar-policy
        working-directory: head
